# ykvos-agent 环境变量样例（规格 §3 / §9 安全规范：密码不落 config.yaml）
# 用法：复制为 /opt/ykvos-agent/.env 后填值；由 agent 进程启动时读取。
#
#   1) 直接写明文密码，例如 YK_MYSQL_PWD='21Hnykxx@2026'
#      重要：如果密码中含有特殊字符（例如 @, #, $, %, *），强烈建议使用单引号括起来。
#      特别地，若含有 '#' 且不加引号会被 .env 解析器误判为注释，含有 '$' 时会被 Shell 误判为变量替换。
#   2) 写"密钥文件路径"，例如 YK_MYSQL_PWD=/etc/ykvos/secrets/mysql
#      ——若值指向一个存在的普通文件，agent 会读取该文件内容作为密码（生产推荐，天然免疫特殊字符）。

# ── VOS MySQL 只读源密码 ──
# 测试环境账号 user=vosdev（见 config.yaml），密码在此注入。
# 生产替换为专用只读账号（仅 GRANT SELECT ON vos3000.*）的密码。
YK_MYSQL_PWD=

# ── Kafka SASL 密码（仅当 config.yaml 中 kafka.sasl.mechanism 非空时生效）──
# 留空即可；SCRAM-SHA-512 等启用时才需填。
YK_KAFKA_PWD=

# ── 入站拉取端点鉴权 Token（config.yaml server.listen 非空时生效）──
# 测试拓扑 VOS 开了公网 5233 端口 → 强烈建议在此设值，否则端点不鉴权！
# 平台侧 pull_client.py 用 --token 传入相同值（Bearer）。
YK_SERVER_TOKEN=
